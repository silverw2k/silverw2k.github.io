// Animal 클래스
class Animal {
    void makeSound() {
        System.out.print("ㅁㅁㄲㄲ...");
    }
}

// Dog 클래스
class Dog extends Animal {
    @Override
    void makeSound() {
        System.out.print("멍멍");
    }
}

// AnimalTest 클래스
public class AnimalTest {
    public static void main(String[] args) {
        // Animal 객체 생성
        Animal animal = new Animal();
        animal.makeSound(); // 결과: ㅁㅁㄲㄲ...

        // Dog 객체 생성
        Dog dog = new Dog();
        dog.makeSound(); // 결과: 멍멍
    }
}
